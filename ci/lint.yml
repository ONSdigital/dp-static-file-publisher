---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: latest

inputs:
  - name: dp-upload-service

run:
  path: dp-static-file-publisher/ci/scripts/lint.sh